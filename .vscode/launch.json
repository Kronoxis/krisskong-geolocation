{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Start",
            "request": "launch",
            "command": "yarn run start",
            "cwd": "${workspaceFolder}"
        }
    ]
}